#pragma once

#include <string>

namespace Alarm {
    constexpr int SNOOZE_TIME_SECONDS = 10;
    extern const std::string DEFAULT_BINDING_ADDRESS;
    extern const unsigned short DEFAULT_BINDING_PORT;
}
